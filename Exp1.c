#include<stdio.h>
int main()
{   
    int T;
    char s[10000];
    scanf("%d", &n);

    for (int i = 1; i <= T; i++)
    {
        scanf("%s", &s);
        if (/* condition */)
        {
            /* code */
        }
        
        printf("%s", )
    }
    
    
    scanf("%s", &n);
    
    return 0;  
}