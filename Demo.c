#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int a;
    char t[100];
    scanf("%d", &a);
    printf("%d", a);
    gets(t);
    puts(t);  
    return 0;
}